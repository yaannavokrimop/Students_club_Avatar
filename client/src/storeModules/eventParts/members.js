const state = {
    members: {
        invited: '',
        contact: '',
        sideOrganizers: '',
        organisers: [],
    }
};

const mutations = {

};

const actions = {

};

const getters = {
    members: state => state.members
};

export default {
    state,
    mutations,
    actions,
    getters
};